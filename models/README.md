# Model Library Policy

Do not commit unknown-license vendor models. Track requested models and source
metadata in `manifest.toml`, then install/download them through explicit tooling
once redistribution terms are clear.
